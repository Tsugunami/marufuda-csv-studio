# 丸札CSVスタジオ (marufuda-csv-studio)

工事現場向け **丸札（機器行き先ラベル）** 作成支援ツール。

A-ONE ラベル屋さん™ の **差し込み印刷機能** で読み込める CSV を、Excel マクロを使わずにモダンなデスクトップアプリで作成する。

> 従来の `KintoneJS_Single/丸札マクロ` (VBA) を Windows ネイティブ化・環境非依存化した後継ツール。

---

## ✨ 特長

- **Excel 不要** — Tauri + React で動く軽量デスクトップアプリ
- **ブロック構造エディタ** — `列×行` のブロック格子と「1ラベルあたりの行数」を UI で柔軟設定
- **「～」デリミタ位置の制御** — 偶数行時に「自分側 / 相手側」のどちらに寄せるかを選択可能（奇数行は中央）
- **反転コピー機能** — 自機器 ↔ 相手機器 の表示入れ替えをワンクリックで隣接ラベルへ転記
- **全体ビュー** — ブロック全体をズーム可能なキャンバスで俯瞰、クリックでラベル選択
- **CSV 出力** — Shift-JIS / UTF-8(BOM) / UTF-8 を切替可能、ラベル屋さん差し込み印刷にそのまま利用可能
- **プリセット** — よく使うレイアウトを保存・再利用

---

## 🧩 ユースケース

工事現場で丸札（機器の行き先を示す札）を作成する際、1枚のラベルに「自機器〜相手機器」の情報を表示する。相手側にも同じラベルを貼るため、表示を入れ替えたラベルをもう1枚作る必要がある。

本ツールでは:

1. ブロック格子の全体ビューから対象ラベルを選ぶ
2. 右側のエディタで行データを入力
3. **反転ボタン** で「～」を境に入れ替えたデータを隣ラベルへ自動転記
4. 全ラベル入力後、**CSV出力** で1ラベル=1行のフラットな CSV を生成
5. 出力した CSV を **ラベル屋さん™ ソフト** の差し込み印刷で読み込み、ラベル用紙に印刷

---

## 🏗️ アーキテクチャ

| 層 | 技术 |
|---|---|
| デスクトップフレームワーク | Tauri 2.x |
| フロントエンド | React 18 + TypeScript |
| ビルド | Vite |
| 状態管理 | Zustand |
| UI | Tailwind CSS + Mantine |
| 全体ビュー描画 | HTML Canvas |
| CSV 出力 (Shift-JIS) | Rust (encoding_rs) |

### ディレクトリ構成

```
marufuda-csv-studio/
├── src-tauri/                  # Rust バックエンド
│   ├── src/
│   │   ├── main.rs
│   │   ├── csv_export.rs       # CSV 書き出し
│   │   └── lib.rs
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/                        # React フロント
│   ├── components/
│   │   ├── OverviewCanvas.tsx  # 全体ビュー
│   │   ├── LabelEditor.tsx     # ラベル編集
│   │   ├── LayoutConfigPanel.tsx
│   │   ├── ReverseToolbar.tsx
│   │   └── OutputPreview.tsx
│   ├── lib/
│   │   ├── types.ts
│   │   ├── reverse.ts
│   │   ├── delimiter.ts
│   │   ├── csv-build.ts
│   │   ├── presets.ts
│   │   └── store.ts
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── vite.config.ts
└── README.md
```

---

## 📐 ブロック構造と「～」挿入ロジック

### ブロック構造

```
シート全体 = blockCols × blockRows の格子
各セル = 1ラベル（itemsPerLabel 行のデータを保持）

例: blockCols=5, blockRows=8, itemsPerLabel=7
→ 5×8 = 40 ラベル、各ラベル 7 行
```

### 「～」の挿入位置

| itemsPerLabel | delimiterAlign | ～の行 index (0始まり) |
|---|---|---|
| 奇数 (例: 7) | (強制 center) | `floor(n/2)` = 3 |
| 偶数 (例: 6) | `self` (自分側=上に寄せる) | `n/2 - 1` = 2 |
| 偶数 (例: 6) | `partner` (相手側=下に寄せる) | `n/2` = 3 |

#### 6行の例

**`delimiterAlign = 'partner'`（～を相手側に寄せる）**
```
あああ      ← 0 自分
いいい      ← 1 自分
ううう      ← 2 自分
～          ← 3
えええ      ← 4 相手
おおお      ← 5 相手
```

**`delimiterAlign = 'self'`（～を自分側に寄せる）**
```
あああ      ← 0 自分
いいい      ← 1 自分
～          ← 2
ううう      ← 3 相手
えええ      ← 4 相手
おおお      ← 5 相手
```

---

## 🔄 反転コピー

選択ラベルの「～」を境に前後を入れ替え、隣接ラベル（右/左/上/下）へ値のみ転記する。

```
元ラベル:        反転先ラベル:
あああ           えええ
いいい           おおお
ううう           (空)
～               (空)
えええ           ううう
おおお           いいい
                 あああ
                 ～
```

---

## 📄 CSV 出力仕様

- 1ラベル = 1行
- 列順: ラベルの行0, 行1, ..., 行N
- ヘッダ: 既定 `項目1..項目N`（カスタマイズ可、省略可）
- エスケープ: RFC 4180 準拠（カンマ / `"` / 改行を含む場合は `"..."` で囲む）
- 文字コード: **Shift-JIS**（既定・ラベル屋さん互換）/ UTF-8 BOM 付き / UTF-8

---

## 🚀 セットアップ

### 前提

- Node.js 18+
- Rust (rustup でインストール)
- Windows: Microsoft Visual Studio C++ Build Tools / WebView2

### インストール

```bash
npm install
```

### 開発起動

```bash
npm run tauri dev
```

### ビルド

```bash
npm run tauri build
```

---

## 📦 プリセット（初期同梱）

| プリセット名 | blockCols | blockRows | itemsPerLabel | delimiter | align |
|---|---|---|---|---|---|
| 通常7行70面 | 7 | 10 | 7 | ～ | center |
| KDDI 8行40面 | 5 | 8 | 8 | ～ | center |
| 95面5行 | 5 | 19 | 5 | ～ | center |
| 95面4行 | 5 | 19 | 4 | ～ | center |
| 横長4行 | 5 | 8 | 4 | ～ | center |
| 6行(自分寄せ) | 5 | 8 | 6 | ～ | self |
| 6行(相手寄せ) | 5 | 8 | 6 | ～ | partner |

---

## 🗺️ ロードマップ

- **Phase 1**（最小構成）: レイアウト設定 + 全体ビュー + ラベルエディタ + 反転 + CSV 出力
- **Phase 2**: プリセット保存/読込、複数選択一括反転、出力プレビュー強化
- **Phase 3**: プロジェクト保存、履歴、キーボードショートカット

---

## 📜 ライセンス

MIT

---

## 🙏 謝辞

- A-ONE / ラベル屋さん™ は 3M 社の商標です。本ツールは A-ONE 社および 3M 社とは関係ありません。
- 本ツールはラベル屋さん™ ソフトの差し込み印刷機能で読み込める CSV を生成するものであり、ラベル屋さん™ ソフト自体を代替するものではありません。
